import vars from '../../utils/vars';

export default {
    container: {
        flex: 1,
        backgroundColor: vars.bgColor,
        padding: 15
    },
    txtContactNumber: {
        marginTop:15,
        marginBottom: 10,
        flex: 1,
        flexDirection: 'row'
        // alignItems: 'center',
        // justifyContent: 'flex-start'
      }
}